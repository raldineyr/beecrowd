#include <stdio.h>
#include <stdlib.h>

int main()
{
    puts("Hickory, dickory, dock");
    puts("The mause ran up the clock");
    puts("The clock stuck one");
    puts("The mouse ran down");
    puts("Hickory, dickory, dock");
    return (0);
}
